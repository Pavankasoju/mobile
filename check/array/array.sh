#!/bin/bash

#defining array with few values

array=(apple banana mango grapes)

#we can get array value by ${array[specific number]}

echo ${array[0]}

# to get entire values of array ${array[*]}

echo ${array[*]}

